# ===== Web 框架 / ASGI 服务器 =====
fastapi==0.115.6
uvicorn[standard]==0.34.0       # 生产 ASGI 服务器（Docker CMD 用其启动）
python-multipart==0.0.20        # 文件上传 UploadFile/File 必需

# ===== 数据校验 =====
pydantic==2.10.4                # FastAPI 会自带兼容版本的 starlette，无需单独声明

# ===== 数据库（异步 MySQL）=====
SQLAlchemy[asyncio]==2.0.36     # 含 greenlet，支持 async/await
aiomysql==0.2.0                 # mysql+aiomysql 异步驱动（自带 PyMySQL）
cryptography==44.0.0            # MySQL 8 默认 caching_sha2_password 认证需要

# ===== 缓存（异步 Redis）=====
redis==5.2.1                    # 使用 redis.asyncio

# ===== 密码加密 =====
passlib==1.7.4
bcrypt==4.0.1                   # 固定 4.0.1，避免新版 bcrypt 与 passlib 1.7.4 不兼容

# ===== 阿里云短信（手机验证码）=====
aliyun-python-sdk-core==2.16.0
